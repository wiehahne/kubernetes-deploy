apiVersion: v1
kind: Service
metadata:
  name: "demoapp-nodejs"
  namespace: "prod"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: "demoapp-nodejs"
