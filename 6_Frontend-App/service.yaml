apiVersion: v1
kind: Service
metadata:
  name: "travel-frontend"
  namespace: "prod"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: "travel-frontend"